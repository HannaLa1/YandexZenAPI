databaseChangeLog:
  - changeSet:
      id: create-like
      author: Anna
      changes:
        - createTable:
            tableName: likes
            columns:
              - column:
                  name: id
                  type: bigint
                  autoIncrement: true
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: name_of_user
                  type: varchar(100)
                  constraints:
                    nullable: false
              - column:
                  name: post_id
                  type: bigint
                  constraints:
                    foreignKeyName: post_id_fk2
                    referencedTableName: posts
                    referencedColumnNames: id
                    deleteCascade: true