databaseChangeLog:
  - changeSet:
      id: create-user
      author: Anna
      changes:
        - createTable:
            tableName: users
            columns:
              - column:
                  name: id
                  type: bigint
                  autoIncrement: true
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: username
                  type: varchar(100)
                  constraints:
                    nullable: false
              - column:
                  name: password
                  type: varchar(100)
                  constraints:
                    nullable: false
              - column:
                  name: firstName
                  type: varchar(100)
                  constraints:
                    nullable: false
              - column:
                  name: lastName
                  type: varchar(100)
                  constraints:
                    nullable: false
              - column:
                  name: email
                  type: varchar(100)
                  constraints:
                    nullable: false
              - column:
                  name: country
                  type: varchar(100)
                  constraints:
                    nullable: false
              - column:
                  name: birthDate
                  type: date
                  constraints:
                    nullable: false
              - column:
                  name: subscriberCounter
                  type: int(10)
              - column:
                  name: status
                  type: varchar(100)
                  constraints:
                    nullable: false